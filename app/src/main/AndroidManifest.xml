<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões necessárias para o aplicativo funcionar-->
    <!-- Permissão para acessar a internet, essencial para Firebase e Open Finance -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permissão para o serviço de leitura de notificações -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinGuia"
        android:name=".FinGuiaApplication"
        tools:ignore="MissingClass">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FinGuia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Serviço de Leitura de Notificações -->
        <service android:name=".service.NotificationReaderService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Adicionar Firebase aqui -->
        <!-- O Firebase adiciona automaticamente algumas configurações via plugin do Gradle.
             No entanto, se você precisar de configurações específicas ou de um API Key manual,
             elas seriam adicionadas aqui como tags <meta-data>. | Feito por I.A -->

        <!-- <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_ic_notification" /> -->
        <!-- <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" /> -->

        <!-- Para fazer logo apos a API da Open Finance -->
        <!-- Configuração de Segurança de Rede (para Open Finance) | Apartir daqui, I.A, pelo menos baseado-->
        <!-- Se a API do Open Finance exigir configurações de segurança de rede personalizadas
             (como permitir tráfego HTTP em debug ou configurar certificados específicos),
             você precisará de um arquivo network_security_config.xml em res/xml/.
             Então, você o referenciaria aqui: -->
        <!-- <meta-data
            android:name="android.security.net.config"
            android:resource="@xml/network_security_config" /> -->

    </application>

</manifest>